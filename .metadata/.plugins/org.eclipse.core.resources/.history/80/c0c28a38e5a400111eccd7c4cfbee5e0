/*
 * bmp585.c
 *
 *  Created on: Aug 8, 2026
 *      Author: crook
 */

#include "bmp585.h"

extern SPI_HandleTypeDef hspi1; // defined in main.c

uint8_t BMP585_ReadReg(uint8_t reg_addr)
{
  uint8_t tx_byte = (1 << 7) | (reg_addr & ~(1 << 7));
  uint8_t rx_byte = 0;

  HAL_GPIO_WritePin(BMP_CS_GPIO_Port, BMP_CS_Pin, GPIO_PIN_RESET);
  HAL_SPI_Transmit(&hspi1, &tx_byte, 1, HAL_MAX_DELAY);
  HAL_SPI_Receive(&hspi1, &rx_byte, 1, HAL_MAX_DELAY);
  HAL_GPIO_WritePin(BMP_CS_GPIO_Port, BMP_CS_Pin, GPIO_PIN_SET);

  return rx_byte;
}

BMP585_Data BMP585_Extract_Data(BMP585_Data_t* data)
{
	//FIFO_Conf_Byte = BMP585_ReadReg(0x18);
	//Extract Altitude

	int8_t TEMP_XLSB =  BMP585_ReadReg(0x1D);
	int8_t TEMP_LSB  =  BMP585_ReadReg(0x1E);
	int8_t TEMP_MSB  =  BMP585_ReadReg(0x1F);

	//Extract Pressure
	int8_t PRES_XLSB = BMP585_ReadReg(0x20);
	int8_t PRES_LSB =  BMP585_ReadReg(0x21);
	int8_t PRES_MSB =  BMP585_ReadReg(0x22);

	//Combine into Struct and Return
	data->




}






